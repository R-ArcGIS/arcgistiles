% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/export-tiles.R
\name{estimate_export_tiles_size}
\alias{estimate_export_tiles_size}
\title{Estimate the size of a tile export}
\usage{
estimate_export_tiles_size(
  x,
  bbox = NULL,
  levels,
  export_by = "LevelID",
  tile_package = TRUE,
  area_of_interest = NULL,
  interval = 2,
  timeout = 3600,
  error_call = rlang::caller_env()
)
}
\arguments{
\item{x}{A \link{MapServer} or \link{VectorTileServer}.}

\item{bbox}{A \code{bbox}, an object with a bounding box, or a length four
numeric \code{c(xmin, ymin, xmax, ymax)}.}

\item{levels}{Integer or string. Levels to export, either a vector of level
ids or a range such as \code{"1-4,7-9"}.}

\item{export_by}{String or \code{\link[=export_by]{export_by()}}. How \code{levels} is interpreted.}

\item{tile_package}{Bool. Export a tile package rather than a cache raster
dataset. Map services only.}

\item{area_of_interest}{An \code{sf} or \code{sfc} polygon. Supersedes \code{bbox}.}

\item{interval}{Double. Seconds between status polls.}

\item{timeout}{Double. Seconds to wait before giving up.}

\item{error_call}{The execution environment of a currently
running function, e.g. \code{caller_env()}. The function will be
mentioned in error messages as the source of the error. See the
\code{call} argument of \code{\link[rlang:abort]{abort()}} for more information.}
}
\value{
A list with the estimated \code{size} in bytes and \code{tile_count}.
}
\description{
Submits the same request as \code{\link[=export_tiles_job]{export_tiles_job()}} but returns an estimated
download size instead of tiles. Map services only.
}
\examples{
\dontrun{
ms <- map_server(world_imagery_url())

estimate_export_tiles_size(ms, ms@full_extent, levels = 0:5)
}
}
\seealso{
Other tile export:
\code{\link[=export_tiles]{export_tiles()}},
\code{\link[=export_tiles_job]{export_tiles_job()}},
\code{\link[=write_tile_package]{write_tile_package()}}
}
\concept{tile export}
