% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/map-series.R
\name{map_grid}
\alias{map_grid}
\title{Build a grid of map pages}
\usage{
map_grid(
  bbox,
  nrow = 2L,
  ncol = 2L,
  overlap = 0,
  crs = NULL,
  error_call = rlang::caller_env()
)
}
\arguments{
\item{bbox}{A \code{bbox}, an object with a bounding box, or a length four
numeric \code{c(xmin, ymin, xmax, ymax)}.}

\item{nrow, ncol}{Integer. Number of page rows and columns.}

\item{overlap}{Double. Fraction of a page to expand each extent by, so
adjacent pages share a margin. \code{0.05} adds 5 percent on every side.}

\item{crs}{Coordinate reference system of the pages. Defaults to the CRS of
\code{bbox}.}

\item{error_call}{The execution environment of a currently
running function, e.g. \code{caller_env()}. The function will be
mentioned in error messages as the source of the error. See the
\code{call} argument of \code{\link[rlang:abort]{abort()}} for more information.}
}
\value{
A data frame with columns \code{page}, \code{row}, \code{col}, and \code{extent}, a
\code{\link[wk:rct]{wk::rct()}} vector of page extents.
}
\description{
Splits \code{bbox} into a regular grid of page extents, the starting point for a
map series.
}
\examples{
map_grid(c(-104, 35.6, -94.32, 41), nrow = 2, ncol = 3)
}
\seealso{
Other map series:
\code{\link[=map_series]{map_series()}}
}
\concept{map series}
