% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/export-map.R
\name{export_map}
\alias{export_map}
\title{Export a map image}
\usage{
export_map(
  x,
  bbox,
  size = c(800L, 600L),
  format = "png",
  file = NULL,
  crs = NULL,
  layers = NULL,
  visibility = "show",
  layer_defs = NULL,
  dpi = 96L,
  transparent = FALSE,
  rotation = NULL,
  time = NULL,
  ...,
  error_call = rlang::caller_env()
)
}
\arguments{
\item{x}{A \link{MapServer}.}

\item{bbox}{A \code{bbox}, an object with a bounding box, or a length four
numeric \code{c(xmin, ymin, xmax, ymax)}.}

\item{size}{Integer. Length two \code{c(width, height)} in pixels.}

\item{format}{String or \code{\link[=image_format]{image_format()}}. Output image format. \code{"png"} is
a single paletted band, so use \code{"png32"} for RGBA bands.}

\item{file}{String. Where to write the image. Defaults to a temporary file.}

\item{crs}{Coordinate reference system of the output image. Defaults to the
service's own.}

\item{layers}{Integer. Layer ids to apply \code{visibility} to.}

\item{visibility}{String or \code{\link[=layer_visibility]{layer_visibility()}}. How \code{layers} is applied.}

\item{layer_defs}{Named list. Definition expressions keyed by layer id.}

\item{dpi}{Integer. Device resolution of the image.}

\item{transparent}{Bool. Render the background transparent.}

\item{rotation}{Double. Degrees to rotate the map counterclockwise.}

\item{time}{Double. Length one or two epoch milliseconds.}

\item{...}{Additional query parameters passed to the service.}

\item{error_call}{The execution environment of a currently
running function, e.g. \code{caller_env()}. The function will be
mentioned in error messages as the source of the error. See the
\code{call} argument of \code{\link[rlang:abort]{abort()}} for more information.}
}
\value{
A \link{MapImage}.
}
\description{
Renders an extent of a map service to an image file. The server expands the
requested extent to the aspect ratio of \code{size}, so the returned
\link{MapImage} carries the extent that was actually drawn.
}
\examples{
\dontshow{if (curl::has_internet()) withAutoprint(\{ # examplesIf}
ms <- map_server(census_url())

# counties layer only, filtered to the large ones
export_map(
  ms,
  c(-104, 35.6, -94.32, 41),
  size = c(600, 400),
  layers = 3,
  layer_defs = list(`3` = "POP2000 > 1000000")
)
\dontshow{\}) # examplesIf}
}
\seealso{
Other map images:
\code{\link[=MapImage]{MapImage()}}
}
\concept{map images}
