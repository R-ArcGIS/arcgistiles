% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tiles.R
\name{get_tiles}
\alias{get_tiles}
\title{Download map tiles}
\usage{
get_tiles(
  x,
  bbox,
  level = NULL,
  size = c(1024L, 1024L),
  dir = tempfile("tiles"),
  progress = TRUE,
  error_call = rlang::caller_env()
)
}
\arguments{
\item{x}{A \link{MapServer} or \link{VectorTileServer}.}

\item{bbox}{A \code{bbox}, an object with a bounding box, or a length four
numeric \code{c(xmin, ymin, xmax, ymax)}.}

\item{level}{Integer. Level of detail. Defaults to the level that renders
\code{bbox} closest to \code{size} pixels.}

\item{size}{Integer. Length two \code{c(width, height)} used to choose \code{level}.}

\item{dir}{String. Directory to write tiles into. Created if needed.}

\item{progress}{Bool. Show a download progress bar.}

\item{error_call}{The execution environment of a currently
running function, e.g. \code{caller_env()}. The function will be
mentioned in error messages as the source of the error. See the
\code{call} argument of \code{\link[rlang:abort]{abort()}} for more information.}
}
\value{
A \link{TileSet}.
}
\description{
Downloads every cached tile covering \code{bbox} at \code{level}. Tiles that are
missing from the cache are reported in the \code{ok} column rather than erroring.
}
\examples{
\dontshow{if (curl::has_internet()) withAutoprint(\{ # examplesIf}
ms <- map_server(world_imagery_url())
boston <- wk::rct(-71.2, 42.3, -71.0, 42.4, crs = 4326)

tiles <- get_tiles(ms, boston, size = c(1024, 1024), progress = FALSE)
tiles
\dontshow{\}) # examplesIf}
}
\seealso{
Other tiles:
\code{\link[=TileSet]{TileSet()}},
\code{\link[=as_rast]{as_rast()}},
\code{\link[=tilemap]{tilemap()}},
\code{\link[=tileset_bbox]{tileset_bbox()}}
}
\concept{tiles}
