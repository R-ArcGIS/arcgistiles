% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read-vector-tiles.R
\name{read_vector_tiles}
\alias{read_vector_tiles}
\title{Read vector tiles into sf}
\usage{
read_vector_tiles(
  x,
  layers = NULL,
  crs = NULL,
  error_call = rlang::caller_env()
)
}
\arguments{
\item{x}{A \link{TileSet} of vector tiles, from \code{\link[=get_vector_tiles]{get_vector_tiles()}}.}

\item{layers}{Character. Layers to read. Defaults to every layer present.}

\item{crs}{Coordinate reference system to return. Defaults to \code{EPSG:4326},
which is what the tiles decode to.}

\item{error_call}{The execution environment of a currently
running function, e.g. \code{caller_env()}. The function will be
mentioned in error messages as the source of the error. See the
\code{call} argument of \code{\link[rlang:abort]{abort()}} for more information.}
}
\value{
A named list of \code{sf} data frames, one per layer.
}
\description{
Decodes downloaded \code{.pbf} tiles and binds each layer across tiles. Requires
the protolite package.
}
\details{
Features are stored clipped to their tile, so a road crossing a tile
boundary arrives as one feature per tile.
}
\examples{
\dontshow{if (curl::has_internet() && rlang::is_installed("protolite")) withAutoprint(\{ # examplesIf}
vts <- vector_tile_server(open_street_map_url())
boston <- wk::rct(-71.07, 42.35, -71.05, 42.36, crs = 4326)
tiles <- get_vector_tiles(vts, boston, level = 14L, progress = FALSE)

roads <- read_vector_tiles(tiles, layers = "road", crs = 3857)
roads$road
\dontshow{\}) # examplesIf}
}
\seealso{
Other vector tiles:
\code{\link[=get_vector_tiles]{get_vector_tiles()}},
\code{\link[=vector_tile_font]{vector_tile_font()}},
\code{\link[=vector_tile_fonts]{vector_tile_fonts()}},
\code{\link[=vector_tile_layers]{vector_tile_layers()}},
\code{\link[=vector_tile_resources]{vector_tile_resources()}},
\code{\link[=vector_tile_sprite]{vector_tile_sprite()}},
\code{\link[=vector_tile_style]{vector_tile_style()}}
}
\concept{vector tiles}
