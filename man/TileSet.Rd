% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tiles.R
\name{TileSet}
\alias{TileSet}
\title{Downloaded tiles}
\usage{
TileSet(
  tiles = (structure(function (.data = list(), row.names = NULL) 
 {
     if
    (is.null(row.names)) {
         list2DF(.data)
     }
     else {
         out <-
    list2DF(.data, length(row.names))
         attr(out, "row.names") <- row.names
      
      out
     }
 }, class = "S7_constructor"))(),
  crs = (structure(function (.data) 

    stop2(sprintf("S3 class <\%s> doesn't have a constructor.", "crs"), call = NULL),
    class = "S7_constructor"))(),
  level = NA_integer_,
  format = NA_character_,
  url = NA_character_
)
}
\arguments{
\item{tiles}{Data frame. One row per tile with columns \code{level}, \code{row},
\code{col}, \code{xmin}, \code{ymin}, \code{xmax}, \code{ymax}, \code{path}, and \code{ok}.}

\item{crs}{Coordinate reference system of the tiles.}

\item{level}{Integer. Level of detail.}

\item{format}{String. Tile format.}

\item{url}{String. Service the tiles came from.}
}
\value{
A \code{TileSet} object.
}
\description{
A set of tiles on disk. Use \code{\link[=get_tiles]{get_tiles()}} or \code{\link[=get_vector_tiles]{get_vector_tiles()}} to create
one.
}
\seealso{
Other tiles:
\code{\link[=as_rast]{as_rast()}},
\code{\link[=get_tiles]{get_tiles()}},
\code{\link[=tileset_bbox]{tileset_bbox()}}
}
\concept{tiles}
