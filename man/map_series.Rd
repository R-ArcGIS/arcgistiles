% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/map-series.R
\name{map_series}
\alias{map_series}
\title{Export a series of map images}
\usage{
map_series(
  x,
  pages,
  size = c(800L, 600L),
  format = "png",
  dir = tempfile("map-series"),
  page_names = NULL,
  margin = 0,
  crs = NULL,
  layers = NULL,
  visibility = "show",
  layer_defs = NULL,
  dpi = 96L,
  transparent = FALSE,
  progress = TRUE,
  ...,
  error_call = rlang::caller_env()
)
}
\arguments{
\item{x}{A \link{MapServer}.}

\item{pages}{A \code{\link[wk:rct]{wk::rct()}}, a data frame with an \code{extent} column or with
\code{xmin}, \code{ymin}, \code{xmax}, and \code{ymax} columns, or any geometry
\code{\link[wk:wk_envelope]{wk::wk_envelope()}} understands. One image is exported per page.}

\item{size}{Integer. Length two \code{c(width, height)} in pixels.}

\item{format}{String or \code{\link[=image_format]{image_format()}}. Output image format. \code{"png"} is
a single paletted band, so use \code{"png32"} for RGBA bands.}

\item{dir}{String. Directory to write images into. Created if needed.}

\item{page_names}{Character. File name stems, one per page. Defaults to
\code{page-001}, \code{page-002}, and so on.}

\item{margin}{Double. Fraction to expand each page extent by before
exporting.}

\item{crs}{Coordinate reference system of the output image. Defaults to the
service's own.}

\item{layers}{Integer. Layer ids to apply \code{visibility} to.}

\item{visibility}{String or \code{\link[=layer_visibility]{layer_visibility()}}. How \code{layers} is applied.}

\item{layer_defs}{Named list. Definition expressions keyed by layer id.}

\item{dpi}{Integer. Device resolution of the image.}

\item{transparent}{Bool. Render the background transparent.}

\item{progress}{Bool. Show a download progress bar.}

\item{...}{Additional query parameters passed to the service.}

\item{error_call}{The execution environment of a currently
running function, e.g. \code{caller_env()}. The function will be
mentioned in error messages as the source of the error. See the
\code{call} argument of \code{\link[rlang:abort]{abort()}} for more information.}
}
\value{
A data frame with one row per page and columns \code{page}, \code{name},
\code{path}, \code{ok}, \code{extent}, and \code{scale}. \code{extent} is what the server drew.
}
\description{
Exports one image per page extent, the equivalent of a map series or
data driven pages. Requests run in parallel.
}
\examples{
\dontshow{if (curl::has_internet()) withAutoprint(\{ # examplesIf}
ms <- map_server(census_url())
pages <- map_grid(c(-104, 35.6, -94.32, 41), nrow = 1, ncol = 2)

# every page comes back at the same scale, which is the point of a series
map_series(ms, pages, size = c(400, 300), progress = FALSE)
\dontshow{\}) # examplesIf}
}
\seealso{
Other map series:
\code{\link[=map_grid]{map_grid()}}
}
\concept{map series}
