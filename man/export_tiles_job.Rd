% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/export-tiles.R
\name{export_tiles_job}
\alias{export_tiles_job}
\title{Submit a tile export job}
\usage{
export_tiles_job(
  x,
  bbox = NULL,
  levels,
  export_by = "LevelID",
  tile_package = TRUE,
  storage_format = "CompactV2",
  optimize = FALSE,
  compression_quality = NULL,
  area_of_interest = NULL,
  ...,
  error_call = rlang::caller_env()
)
}
\arguments{
\item{x}{A \link{MapServer} or \link{VectorTileServer}.}

\item{bbox}{A \code{bbox}, an object with a bounding box, or a length four
numeric \code{c(xmin, ymin, xmax, ymax)}.}

\item{levels}{Integer or string. Levels to export, either a vector of level
ids or a range such as \code{"1-4,7-9"}.}

\item{export_by}{String or \code{\link[=export_by]{export_by()}}. How \code{levels} is interpreted.}

\item{tile_package}{Bool. Export a tile package rather than a cache raster
dataset. Map services only.}

\item{storage_format}{String or \code{\link[=storage_format]{storage_format()}}. \code{"CompactV2"} yields a
\code{.tpkx}, \code{"Compact"} a \code{.tpk}. Map services only.}

\item{optimize}{Bool. Recompress JPEG tiles to shrink the download.}

\item{compression_quality}{Integer. Compression factor between 0 and 100,
used when \code{optimize} is \code{TRUE}.}

\item{area_of_interest}{An \code{sf} or \code{sfc} polygon. Supersedes \code{bbox}.}

\item{...}{Additional query parameters passed to the service.}

\item{error_call}{The execution environment of a currently
running function, e.g. \code{caller_env()}. The function will be
mentioned in error messages as the source of the error. See the
\code{call} argument of \code{\link[rlang:abort]{abort()}} for more information.}
}
\value{
A \link{TileExportJob}.
}
\description{
Asks a service to package its cache tiles for offline use. The service must
report \code{export_tiles_allowed}.
}
\examples{
\dontrun{
vts <- vector_tile_server(open_street_map_url())
job <- export_tiles_job(vts, service_bbox(vts), levels = 0:3)
}
}
\seealso{
Other tile export:
\code{\link[=TileExportJob]{TileExportJob()}},
\code{\link[=estimate_export_tiles_size]{estimate_export_tiles_size()}},
\code{\link[=export_tiles]{export_tiles()}},
\code{\link[=job_await]{job_await()}},
\code{\link[=job_messages]{job_messages()}},
\code{\link[=job_result]{job_result()}},
\code{\link[=job_status]{job_status()}},
\code{\link[=write_tile_package]{write_tile_package()}}
}
\concept{tile export}
